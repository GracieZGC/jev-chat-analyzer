# ===== 密钥（绝对不能上传） =====
# 真实 .env 在仓库外（../.env，即 2026-09-JEV/.env），不要把它拷进仓库。
# 开源版请在仓库内放 .env.example，并把真实密钥填到你自己本地的 .env。
.env
.env.*
*.env
!.env.example

# ===== WorkBuddy 内部目录（含工作日志 / 项目记忆 / 作者私人信息） =====
.workbuddy/

# ===== 本地改动备份与缓存（中间态，非源码） =====
_backup/
__pycache__/
*.pyc

# ===== macOS 资源叉垃圾文件（每个真实文件旁自动生成的 ._xxx） =====
._*
.DS_Store
.Spotlight-V100
.Trashes

# ===== 运行产物（回流池会自动重建，且可能攒入真实对话样本） =====
low_confidence_pool.json
